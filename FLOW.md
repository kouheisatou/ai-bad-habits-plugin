# 投稿されるまでの流れ

「AIに指摘したら図鑑に貯まる」が、実際にどう動くかを順に書く。

---

## 全体

```
  ① あなたがAIの出力に不満を言う
        「この文、AIっぽいから直して」
          ↓
  ② Claude がスキルを読み込む
        description に「AIっぽさの指摘」と書いてあるので自動で読まれる
          ↓
  ③ Claude が出力を直す（ここは今までどおり）
          ↓
  ④ Claude が「記録しますか」と確認する
        送る内容を全部見せる
          ↓
  ⑤ あなたが承諾する
          ↓
  ⑥ MCP経由で図鑑へ送られる  ← ここで初めて通信が起きる
        record_habit(text, before, after, note)
          ↓
  ⑦ 図鑑にあなたの非公開データとして保存される
        この時点では誰にも見えない
          ↓
  ⑧ 後日、マイページで公開に切り替える（任意）
        ここで初めて他の人に見える
```

**通信が起きるのは⑥だけ。** ①〜⑤はあなたのPCの中で完結する。
承諾しなければ何も送られない。

---

## 各段階で何が起きるか

### ① あなたが指摘する

普段どおりに言うだけでよい。特別なコマンドは要らない。

```
「なんか薄っぺらいな。もっと具体的に書いて」
「この関数名、AIが付けたみたいで気持ち悪い」
「この図、情報詰め込みすぎ」
```

文章でもコードでもUIでも、**AIの出力に対する指摘なら全部**が対象になる。

ただし次は記録されない。癖ではないため。

- 事実の誤りの訂正（「この数字が違う」）
- 仕様や要件の変更（「やっぱりこうして」）
- 環境やツールの問題（「ビルドが通らない」）

### ② スキルが読み込まれる

`SKILL.md` の `description` に「AIの出力への指摘を記録する」と書いてあるので、
Claude が場面を判断して自動で読み込む。あなたが呼び出す必要はない。

### ③ 出力を直す

記録より先に直す。**直すことが本題**で、記録はついでという扱いにしてある。

### ④ 確認が出る

```
図鑑に記録しますか？

  癖   : 抽象語で締めて、具体が無いまま終わる
  修正前: 品質を大切にすることが重要です。
  修正後: レビューで指摘される箇所が月に3件減りました。
  指摘 : なんか薄っぺらい

非公開で記録されます（公開するかは後でマイページから選べます）
```

**毎回出る。** 何が送られるか分からないまま送られることはない。

うるさければ「以降は確認しないで」と言えば、次回から確認なしで記録される
（設定はマイページからいつでも戻せる）。

確認を省いた場合でも、**送ってはいけないもの**（ファイルパス、固有名詞、鍵の類）を
除く判断は省略されない。

### ⑤ 承諾／拒否

- 承諾すれば⑥へ進む
- 断れば何も送らない。**そのセッション中はもう聞かれない**

### ⑥ 送信

MCPサーバー（`https://ai-bad-habit.com/api/mcp`）へ JSON-RPC で送る。

**初回だけ**、ここでブラウザが自動的に開く。
Googleでログインして「許可する」を押すと、エディタが接続用のトークンを受け取る。
以後は自動で更新されるので、二度と操作は要らない。

**送られるもの**

| 項目 | 中身 |
|---|---|
| `text` | 癖の要約（200字以内） |
| `before` | 指摘された箇所 |
| `after` | 直した後 |
| `note` | あなたが言った言葉そのまま |
| `source` | どのモデルの出力か |

**送られないもの**

ファイルパス、リポジトリ名、URL、メールアドレス、
指摘の対象になっていない周辺の文章、未公開の固有名詞。

### ⑦ 保存

`visibility='private'` で入る。図鑑の一覧にもランキングにも出ない。
クラスタリング（似た癖をまとめる処理）にも乗らない。**あなたにしか見えない。**

貯まったものは、自分の改善プロンプトの材料として使える。
公開しなくても価値が出るようにしてある。

### ⑧ 公開（任意）

マイページに一覧が出るので、他の人にも役立ちそうなものだけ「公開する」を押す。

- **MCPからは公開できない。** プラグイン側に公開の手段は用意していない
- 公開するときだけ、メール・URL・パス・長い数字を伏せる
- 公開すると、似た癖どうしがまとめられ、他の人が「それな」を押せるようになる

---

## 通信のタイミング

| いつ | 何が起きるか |
|---|---|
| Claude Code の起動時 | MCPサーバーに接続し、使えるツールの一覧を取る |
| **初めて記録するとき** | **ブラウザが開き、接続の許可を求める（この1回だけ）** |
| 文章を書く前 | 蓄積したナレッジを取りに行くことがある（`get_prompt_knowledge`） |
| **あなたが承諾したとき** | **記録が送られる。ここだけがデータの送信** |
| マイページを開いたとき | ブラウザから自分のデータを読む |

接続を許可していなければ、記録は行われず修正だけが行われる。
プラグインが入っていても、**許可しない限り何も送られない。**

---

## 止め方

- **止める**: https://ai-bad-habit.com/settings/plugin で接続を取り消す
- **記録したものを消す**: マイページから個別に削除する
